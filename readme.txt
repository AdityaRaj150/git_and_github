1. make a folder (it's a normal folder)
2. type git init in bash command terminal(now it is a git repository), u can confirm by using ls -a(it shows hidden file
which also has .git)
3. git status shows the the status of staging area